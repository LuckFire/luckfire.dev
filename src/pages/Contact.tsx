import { Component } from 'preact';

import { Foreground } from '#/components/Foreground';

import '#/styling/pages/projects.scss';

export class Contact extends Component {
    render() {
        return <Foreground>
            
        </Foreground> 
    }
}